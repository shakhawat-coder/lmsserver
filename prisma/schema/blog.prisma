model Blog {
  id        String   @id @default(uuid())
  title     String
  content   String
  image     String?
  author    String   @default("Library Editor")
  category  String   @default("Library News")
  published Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("blogs")
}
